let asideMenu = $('.aside_menu');
let title = asideMenu.find('a');

/* title.each(function(idx, item){
    item.click(function(){
        titleName = $(this).val();
        console.log(titleName)
    })
}) */
title.click(function(){
    /* titleName = $(this).val();
    console.log(titleName) */
})